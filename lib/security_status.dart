/// Represents the different types of security statuses that can be monitored.
enum SecurityStatus {
  /// Indicates a VPN connection is active.
  vpnConnection,

  /// Indicates the device is in debug mode.
  debugMode,

  /// Indicates a USB connection is active.
  usbConnection,
}
